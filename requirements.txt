# Pinned to the versions we run inference with (Python 3.11, CUDA 12.4).
# Install PyTorch first (see README "Installation"), then this file, then
# `pip install -e VeOmni/ --no-deps`. FlashAttention-2 is not required.

# -- core ---------------------------------------------------------------
transformers==4.56.2
datasets==4.0.0
accelerate==1.12.0
numpy==2.0.1
pillow==12.0.0
pyyaml==6.0.3
safetensors==0.7.0
einops==0.8.1
tqdm==4.67.1
sentencepiece==0.2.0
scipy==1.16.3
timm==1.0.22
tensorboard==2.20.0
tiktoken==0.9.0
torchdata==0.11.0
blobfile==3.1.0
psutil==7.1.3
packaging==25.0

# NOTE: flash-attn is required by the AToken visual tokenizer but must be
# installed AFTER torch (see README step 3):
#   pip install flash-attn==2.7.2.post1 --no-build-isolation

# -- inference (AToken detokenizer, video sampling) ---------------------
diffusers==0.31.0
decord==0.6.0
imageio==2.37.2
ftfy==6.3.1
regex==2024.11.6
easydict==1.13
torchmetrics==1.8.2
webdataset==1.0.2

# -- optional: object-storage token loading (not needed for local files) --
# boto3==1.43.55
# petrel-oss-sdk==2.3.25

# -- 3D understanding -----------------------------------------------------
# Required by the bundled GLB/GLTF encoder and the mesh surface-sampling fallback.
trimesh==5.0.0
# Optional: Gaussian-splat/point-cloud extensions for advanced external assets.
# plyfile==1.1.3
# diff-gaussian-rasterization  # install from source: https://github.com/graphdeco-inria/diff-gaussian-rasterization
